//03)​ Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.

function elevaBase(a,b){
    let baseElevada = Math.pow(a,b);
    console.log(baseElevada);
}

elevaBase(15,3);
elevaBase(3,2);
elevaBase(10,2);
elevaBase(5,2);
